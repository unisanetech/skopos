# Fixture architecture overview

The workspace is a pnpm monorepo. `packages/api` owns the HTTP API,
`packages/web` owns the web application, and `packages/shared` owns reusable
cross-package primitives.
