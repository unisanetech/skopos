# Clean service agent rules

- Use the root package scripts as the canonical command surface.
- Keep docs under `docs/`.
- Treat this repo as a small API service unless the user explicitly changes direction.
