# Start Here

Self-hosted tooling fixture docs root.

1. Use the root Skopos config and AGENTS source.
2. Treat `packages/cli`, `packages/core`, and `packages/ui` as the active workspace model.
3. Use workflow manifests under `tools/skopos/workflows/` for self-hosted maintenance actions.
