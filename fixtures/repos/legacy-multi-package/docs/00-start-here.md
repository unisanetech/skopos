# Legacy Multi-Package Fixture

This fixture represents a quieter brownfield repo:

- canonical docs root exists
- AGENTS.md exists
- root workflow commands exist
- package boundaries are still weak because the workspace contract is implicit
