# Allowed semantic-guidance contexts

These examples must pass after project patterns are configured:

- Do not use `retired/package/path`; use the accepted package authority.
- Historically, the starter preset was the default before the cutover.
- The legacy path remains temporarily available until migration completes.
