# Active semantic-guidance drift

These examples must fail after project patterns are configured:

- Use `retired/package/path` for all new modules.
- The starter preset is the recommended default composition.
- The architecture references `retired/package/path`.
