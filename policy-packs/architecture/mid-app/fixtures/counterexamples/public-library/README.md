# Valid public-library counterexample

```text
src/lib.*
src/public-api/**
tests/compatibility/**
```

Exports and compatibility are the product. This is a high-confidence anti-signal for automatic `architecture.mid-app` application; a library-specific policy should govern it.
