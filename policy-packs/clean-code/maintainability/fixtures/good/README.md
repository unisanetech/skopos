# Good Fixture: Readable Feature

This fixture describes the expected shape for clean maintainable code.

Signals:

- feature behavior lives near the feature
- helper names describe their purpose
- shared code has more than one real caller
- boundary data is parsed before core logic
- changed behavior has focused proof

The pack should treat this as a good example, not as a required folder structure.
