const rows=[...document.querySelectorAll('.row')];rows.forEach(row=>row.addEventListener('click',()=>{rows.forEach(item=>item.classList.remove('selected'));row.classList.add('selected');const [title,severity,owner,updated]=row.dataset.detail.split('|');document.querySelector('#detail-title').textContent=title;document.querySelector('#severity').textContent=severity;document.querySelector('#owner').textContent=owner;document.querySelector('#updated').textContent=updated}));document.querySelector('#ack')?.addEventListener('click',()=>document.querySelector('#status').textContent='Incident acknowledged.');
