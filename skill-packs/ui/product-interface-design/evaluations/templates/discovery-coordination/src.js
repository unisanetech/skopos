const cards=[...document.querySelectorAll('.card')];cards.forEach(card=>card.addEventListener('click',()=>document.querySelector('#selected').textContent=card.dataset.name));document.querySelector('#search').addEventListener('input',event=>{const query=event.target.value.toLowerCase();let shown=0;cards.forEach(card=>{card.hidden=!card.textContent.toLowerCase().includes(query);if(!card.hidden)shown++});document.querySelector('#count').textContent=`${shown} workspace${shown===1?'':'s'}`});
