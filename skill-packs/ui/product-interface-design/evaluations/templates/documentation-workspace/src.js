const examples={js:["import { Relay } from '@relay/sdk';","","await relay.send('invoice.created', {","  invoiceId: 'inv_2048'","});"].join('\n'),py:["from relay import Relay","","relay.send('invoice.created', {","  'invoice_id': 'inv_2048'","})"].join('\n')};document.querySelectorAll('[data-lang]').forEach(button=>button.addEventListener('click',()=>document.querySelector('#code code').textContent=examples[button.dataset.lang]));document.querySelector('#copy').addEventListener('click',()=>document.querySelector('#status').textContent='Example copied.');
